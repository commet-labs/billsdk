services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: billsdk
      POSTGRES_PASSWORD: billsdk
      POSTGRES_DB: billsdk
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
